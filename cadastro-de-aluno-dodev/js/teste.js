
let numeros = [1,2,3,4,5,6,7]

let temPar = numeros.some()



/*

class Aluno {
    Nome
    Idade
    Nota
    constructor(nome,idade,nota){
        this.Nome = nome
        this.Idade = idade
        this.Nota = nota
    }
}

let arrayAlunos = []

function CadastrarAluno(nome,idade,array){
    let ObjetoAluno = new Aluno(nome,idade,nota)
    if(!array.some(x => x.Nome == nome)){
        array.push(ObjetoAluno)
    }
    return ObjetoAluno
}

CadastrarAluno('Thalyson',25,10,arrayAlunos)
console.log(CadastrarAluno)

*/